# Test environment variables
# Uses local Docker PostgreSQL instead of Neon

DATABASE_URL=postgresql://postgres:postgres@localhost:5433/saf_rankings_test
TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:5433/saf_rankings_test

# Mock Vercel Blob token (mocked in tests)
BLOB_READ_WRITE_TOKEN=test_token

# Base URL for E2E tests
NEXT_PUBLIC_BASE_URL=http://localhost:3030
